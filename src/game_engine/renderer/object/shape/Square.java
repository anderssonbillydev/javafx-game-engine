package game_engine.renderer.object.shape;

import game_engine.renderer.object.Pixel;

public class Square extends Rectangle {

    public Square(int size, Pixel pixel) {
        super(size, size, pixel);
    }

}
